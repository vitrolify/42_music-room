info:
  name: Get current ranked queue
  type: http
  seq: 2

http:
  method: GET
  url: "{{domain}}{{port}}{{baseUrl}}/events/:eventId/queue"
  params:
    - name: eventId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
