info:
  name: Initial root request
  type: http
  seq: 10

http:
  method: GET
  url: "{{domain}}{{port}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
