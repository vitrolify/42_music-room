info:
  name: Get Metrcis
  type: http
  seq: 10

http:
  method: GET
  url: "{{domain}}{{port}}/metrics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
