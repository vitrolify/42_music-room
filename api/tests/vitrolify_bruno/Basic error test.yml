info:
  name: Basic error test
  type: http
  seq: 9

http:
  method: GET
  url: "{{domain}}{{port}}/error"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
