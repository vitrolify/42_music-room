info:
  name: WebSocket channel endpoint (documented as upgrade route)
  type: http
  seq: 7

http:
  method: GET
  url: "{{domain}}{{port}}{{baseUrl}}/ws/events/:eventId"
  params:
    - name: eventId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
