# infra/docker/compose/docker-compose.test.yml
services:
  db-test:
    image: postgres:16-alpine
    command: ["postgres", "-c", "max_connections=300"]
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: test_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d test_db"]
      interval: 2s
      timeout: 5s
      retries: 10
    networks:
      - vitrolify-test-net

  redis-test:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - vitrolify-test-net
    ulimits:
      nofile:
        soft: 10240
        hard: 10240

  migrations-test:
    build:
      context: ../../../api
      dockerfile: Dockerfile
    working_dir: /api
    environment:
      DATABASE_URL: postgresql+asyncpg://test_user:test_password@db-test:5432/test_db
    depends_on:
      db-test:
        condition: service_healthy
    volumes:
      - ../../../api:/api
    command: ["alembic", "upgrade", "head"]
    networks:
      - vitrolify-test-net

  api-test:
    build:
      context: ../../../api
      dockerfile: Dockerfile
    working_dir: /api
    environment:
      LOG_LEVEL: warning
      DATABASE_URL: postgresql+asyncpg://test_user:test_password@db-test:5432/test_db
      REDIS_URL: redis://redis-test:6379/0
      LOAD_TEST_MODE: "True"
    depends_on:
      db-test:
        condition: service_healthy
      migrations-test:
        condition: service_completed_successfully
    volumes:
      - ../../../api/app:/api/app
      - ../../../api/alembic.ini:/api/alembic.ini:ro
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "6", "--backlog", "8192", "--limit-concurrency", "3000"]
    networks:
      - vitrolify-test-net
    ulimits:
      nofile:
        soft: 10240
        hard: 10240

  k6:
    image: grafana/k6:0.52.0
    depends_on:
      api-test:
        condition: service_started
    volumes:
      - ../../k6:/scripts:ro
    environment:
      K6_API_BASE_URL: http://api-test:8000 
    entrypoint: ["k6"]
    command: ["run", "/scripts/playlist_test.js"]
    networks:
      - vitrolify-test-net
    ulimits:
      nofile:
        soft: 10240
        hard: 10240

networks:
  vitrolify-test-net:
    driver: bridge # Removed internal: true to fix uvloop DNS resolution
